interface Book {
    /*
     * This method will function as a method for the insertion of books, 
     * don't forget that this is abstract and has no body.
     */
    void insertBookList();

    /*
     * This method will function as a method for the insertion of borrowed books,
     * don't forget that this is abstract and has no body.
     */
    void insertBorrowedBooks();

    /*
     * This method will function as a method for the display of books,
     * don't forget that this is abstract and has no body.
     */
    void displayBookList();

    /*
     * This method will function as a method for the display of borrowed books,
     * don't forget that this is abstract and has no body.
     */
    void displayBorrowedBooks();
}